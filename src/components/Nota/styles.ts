import styled from 'styled-components'

import { Props } from '.'

export const NoteContainer = styled.div<Props>`
  text-align: center;
  font-size: 18px;
  font-weight: 700;
  padding: 6px 4px;
  margin-right: 8px;
  display: inline-block;
`
